// push, pop, shift, ushift, splice, slice
const vowels = ["a", "e", "i", "o", "u"]
// return values & mutating original array
console.log(vowels)
// vowels.push("c","d") // add an element to the end of an array
// vowels.pop() // remove the last element from the array
// vowels.shift() // remove the first element of an array 
// vowels.unshift("a","a") // adds elements to the beginning of an array
// console.log(vowels.slice(2)) // slices out elements from index 2 to the end of the array
// let result = vowels.slice(1,4) // ["e","i","o"]
// console.log(result)

// vowels.splice(1,3, "b", "c","d","f")

console.log(vowels.join("")) // joins elements of an array into a string
// console.log(vowels.toString())


console.log(vowels)
